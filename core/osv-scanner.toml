[[IgnoredVulns]]
id = "GHSA-grv7-fg5c-xmjg"
reason = "Vulnerable code is not actually used"

[[IgnoredVulns]]
id = "GHSA-mh99-v99m-4gvg"
reason = "brace-expansion is a dev-only transitive dep (via eslint, jest, rimraf, globby, etc.) used to expand glob patterns supplied by our own configs, not untrusted input. The 1.x and 2.x branches have no backport yet as of this writing; @luigi-project/core ships from core/public/ with zero runtime dependencies so end users never receive brace-expansion. Revisit when 1.1.17 / 2.1.3 are published."
