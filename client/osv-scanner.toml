[[IgnoredVulns]]
id = "GHSA-5c6j-r48x-rmvq"
reason = "False positive: terser-webpack-plugin uses serialize-javascript only for build cache keys (dev-controlled config). Build-time only, no user input."

[[IgnoredVulns]]
id = "GHSA-qj8w-gfj5-8c6v"
reason = "False positive: terser-webpack-plugin uses serialize-javascript only for build cache keys (dev-controlled config). Build-time only, no user input."

[[IgnoredVulns]]
id = "GHSA-mh99-v99m-4gvg"
reason = "brace-expansion is a dev-only transitive dep (via globby, test-exclude) used to expand glob patterns supplied by our own configs, not untrusted input. The 1.x and 2.x branches have no backport yet as of this writing; @luigi-project/client ships from client/public/ with zero runtime dependencies so end users never receive brace-expansion. Revisit when 1.1.17 / 2.1.3 are published."
